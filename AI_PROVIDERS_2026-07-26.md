# Безкоштовні LLM API для наділення позицій Prom — аналіз (26.07.2026)

Джерело: [cheahjs/free-llm-api-resources](https://github.com/cheahjs/free-llm-api-resources) — курований
список безкоштовних інференс-API. Питання: чи придатне це для prom-autopilot, зокрема для
наділення (збагачення) карток Prom.

## Коротко

Так, придатне — і б'є рівно в те місце, де проєкт зараз стоїть. Модуль `adding/ai_layer.py` уже
робить саме наділення: віддає факти BM Parts моделі й отримує 10 текстових полів картки
(назви RU/UA, пошукові запити, HTML-описи, meta). Але канал у нього був один — GitHub Models
через `GH_MODELS_TOKEN`, а це на Copilot Free **10 запитів/хв і 50 запитів на ДОБУ** для
«high»-моделей на кшталт `openai/gpt-4.1`. Тобто повний каталог 3912 позицій — **79 днів**.
Це і є вузьке горло, яке список закриває.

## Скільки коштує один прохід каталогу

Один виклик наділення ≈ **2 000 токенів входу** (артикул, бренд, до 10 OEM, до 10 аналогів,
характеристики, сумісність + системний промпт) і **≈1 500 токенів виходу** (2 назви, 2×30-40
пошукових запитів, 2 HTML-описи, 4 meta-поля). Разом ≈ 3 500 токенів на позицію.

3912 позицій ≈ **7,8 млн вхідних + 5,9 млн вихідних токенів**.

Важливо: пакувати по кілька позицій в один запит майже не вийде — вихід на одну позицію вже
~1 500 токенів, а безкоштовні тарифи ріжуть відповідь на 4 000–8 000 токенів. Реалістично
максимум 2 позиції на запит. Тому нижче рахунок ведеться «1 запит = 1 позиція».

## Хто з провайдерів реально тягне 3912 позицій

Ліміти станом на 26.07.2026 (з README списку та документації провайдерів). «Вузьке місце» —
той ліміт, який упирається першим.

| Провайдер / модель | RPM | RPD | TPM | TPD | Вузьке місце | Позицій/добу | Днів на каталог |
|---|---|---|---|---|---|---|---|
| GitHub Models `gpt-4.1` (Copilot Free) — **як зараз** | 10 | 50 | — | — | RPD | 50 | **79** |
| Google AI Studio `gemma-3-27b-it` | 30 | 14 400 | 15 000 | — | TPM | ~250/год | **3 ночі** |
| Google AI Studio `gemini-3.1-flash-lite` | 15 | 500 | 250 000 | — | RPD | 500 | **8** |
| Google AI Studio `gemini-3.x flash` (не lite) | 5 | 20 | 250 000 | — | RPD | 20 | 196 |
| Groq `llama-3.1-8b-instant` | 30 | 14 400 | 6 000 | 500 K | TPD | 143 | 28 |
| Groq `gpt-oss-120b` / `qwen3.6-27b` | 30 | 1 000 | 8 000 | 200 K | TPD | 57 | 69 |
| Cerebras `gpt-oss-120b` | 5 | — | 30 000 | 1 M | TPD | 285 | 14 |
| Mistral La Plateforme (Experiment) | до 12,5/с | — | 25 K–20 M | — | практично нема | тисячі | **1–2** |
| OpenRouter free | 20 | 50 (1 000 після разового $10) | — | — | RPD | 50 / 1 000 | 79 / **4** |
| NVIDIA NIM | 40 | не задекларовано | — | — | невідоме | треба міряти | ? |
| Cohere | 20 | 1 000 **на місяць** | — | — | місячний | 33 | 118 |
| Cloudflare Workers AI | — | — | 10 000 «нейронів»/добу | — | добовий | десятки | ✗ |

Плюс окремий клас — **разові кредити**: Scaleway (1 млн токенів), Alibaba Model Studio
(1 млн токенів на модель, 90 днів), SambaNova ($5 на 3 міс), Baseten ($30), Modal ($30/міс),
AI21 ($10 на 3 міс). Це не для щоденної роботи, але одного Baseten/Modal вистачає, щоб
одноразово прогнати весь каталог доброю моделлю.

### Висновок по таблиці

Три робочі варіанти для повного бекфілу, і всі безкоштовні:

1. **Gemma 3 27B через Google AI Studio** — найшвидший безкоштовний шлях: RPD 14 400 не заважає,
   упирається TPM 15 000 → ~4 позиції/хв → ~250/год. За 5-годинний прогін GitHub Actions ≈ 1 250
   позицій, тобто **3 ночі на весь каталог**. Мінус — Gemma 3 27B пише українською помітно
   слабше за Gemini Flash: тексти сухіші, іноді калькує з російської.
2. **Mistral Experiment** — найбільша пропускна здатність узагалі, каталог за 1–2 прогони.
   Мінус серйозний: безкоштовний тариф **вимагає згоди на навчання на ваших даних** (обов'язкова
   умова, не опція) плюс верифікація по телефону.
3. **Gemini 3.5 Flash-Lite** — найкраща якість української серед безкоштовних, 500 позицій/добу
   → 8 ночей. Нуль зусиль з налаштування, просто працює.

А для **щоденної доливки** — тобто нових позицій із «Огляд_Додавання», яких десятки на день, а
не тисячі — безкоштовного Gemini 3.5 Flash-Lite (500/добу) вистачає назавжди з великим запасом.

## Чесна арифметика: а може, просто заплатити

Той самий повний бекфіл на платному тарифі:

| Модель | Стандарт | Batch API (−50%) |
|---|---|---|
| `gemini-2.5-flash-lite` ($0.10/$0.40 за 1M) | **≈ $3,1** | ≈ $1,6 |
| `gemini-3.1-flash-lite` ($0.30/$2.50 за 1M) | ≈ $17 | ≈ $8,5 |

Тобто повний одноразовий прохід 3912 позицій найдешевшою Flash-Lite коштує **близько трьох
доларів**. Воювати з безкоштовними лімітами три тижні заради $3 — погана угода. Моя рекомендація:
безкоштовні канали брати як **щоденний робочий режим і як резервні сходи**, а разовий бекфіл
зробити або за 3 ночі на Gemma, або за $3–5 на платній Flash-Lite, якщо якість Gemma не влаштує.

Зверни увагу на `gemini-2.5-flash-lite`: безкоштовно там лише 20 запитів/добу (марно), але як
платна вона у 8 разів дешевша за 3.5 Flash-Lite при цілком пристойній якості для карток запчастин.

## Що вже зроблено в коді

### `adding/ai_layer.py` — сходи провайдерів

Замість одного каналу тепер сходи, які перемикаються на наступного провайдера при 429 / вичерпаній
добовій квоті — за тією ж логікою, що водоспад джерел ціни в репрайсері. Усі безкоштовні
провайдери, крім Anthropic, сумісні з OpenAI `chat/completions`, тож це одна функція плюс таблиця
ендпойнтів:

```python
PROVIDERS = {
    "gemini":     (".../v1beta/openai/chat/completions", "GEMINI_API_KEY",     "gemini-3.1-flash-lite", 4.5),
    "openrouter": ("https://openrouter.ai/api/v1/...",   "OPENROUTER_API_KEY", "google/gemma-3-27b-it:free", 3.2),
    "groq":       ("https://api.groq.com/openai/v1/...", "GROQ_API_KEY",       "openai/gpt-oss-120b", 2.2),
    "cerebras":   ("https://api.cerebras.ai/v1/...",     "CEREBRAS_API_KEY",   "gpt-oss-120b", 12.5),
    "mistral":    ("https://api.mistral.ai/v1/...",      "MISTRAL_API_KEY",    "mistral-small-latest", 1.2),
    "nvidia":     ("https://integrate.api.nvidia.com/...", "NVIDIA_API_KEY",   "meta/llama-3.3-70b-instruct", 1.6),
    "github":     ("https://models.github.ai/inference/...", "GH_MODELS_TOKEN", "openai/gpt-4.1", 6.5),
}
```

Четверте число — мінімальна пауза між запитами, порахована з задекларованого RPM із запасом,
щоб не ловити 429 на рівному місці. Провайдер, який віддав 429/402/403, іде в cooldown
(`AI_COOLDOWN`, типово 15 хв), і робота йде далі наступним — прогін не падає через вичерпану квоту.

Порядок задає `AI_PROVIDERS` (через кому). Порожньо — беруться всі, для яких є ключ, у порядку
`gemini, openrouter, groq, cerebras, mistral, nvidia, github, anthropic`. Немає жодного ключа —
повертається `None`, як і раніше, конвеєр працює без AI. Стара поведінка (`GH_MODELS_TOKEN` +
`AI_MODEL`) збережена повністю.

### Захист від вигаданих номерів — головне

Це найважливіше при переході на слабші безкоштовні моделі. Наявний `validator.py` ловить формат
(порожня назва, дефіс, >110 символів, нема фото), але **не ловить вигадану деталь** — а саме це
роблять моделі рівня 8B–27B: підставляють правдоподібний, але неіснуючий OEM. Для магазину
запчастин це найдорожча помилка з можливих: клієнт отримує не ту деталь.

Тому додано `numbers_ok()`: кожен номероподібний рядок у `name_ru/name_ua/meta_title_*`
нормалізується (тільки цифри й латиниця, у нижній регістр) і звіряється з вхідними фактами —
артикул, OEM, аналоги. Знайшовся номер, якого у фактах не було — картка не береться взагалі.
В описі номери не перевіряються, бо там вони йдуть у зв'язному тексті, але назва — обличчя
картки, і в ній вигадок бути не може. У промпт додано пряму заборону вигадувати номери.

Перевірено:

```
дозволений номер (34116794300 з фактів) -> True
вигаданий номер  (99999123456)          -> False, картку не беру
```

### `workflows/add.yml`

Додано всі ключі провайдерів (усі опціональні — немає секрету, провайдер просто пропускається),
вхід `ai_providers` для вибору сходів руками, `PYTHONUNBUFFERED: "1"` щоб лог було видно наживо,
і `timeout-minutes: 350` під довгі прогони бекфілу.

**Одна зміна поведінки, яку варто підтвердити окремо:** дефолт входу `target` змінено з `export`
на `staging`. Логіка — масовий AI-бекфіл 3912 карток не має литися прямо в бойовий Export без
перегляду. Якщо хочеш лишити як було, це один рядок назад.

`workflows/nightly.yml` не чіпав.

## Що треба зробити тобі

1. Завести ключі (безкоштовно, 2–5 хв кожен) і покласти в GitHub Secrets:
   `GEMINI_API_KEY` (aistudio.google.com), далі за бажанням `OPENROUTER_API_KEY`, `GROQ_API_KEY`,
   `CEREBRAS_API_KEY`. Ключі я не бачу і не вводжу — це робиш ти в Settings → Secrets.
2. Сказати, чи заливати ці зміни в репозиторій (`adding/ai_layer.py` + `workflows/add.yml`).
3. Перед бекфілом — тестовий прогін `MAX=50`, `TARGET=staging` на 2–3 моделях
   (`gemini-3.1-flash-lite`, `gemma-3-27b-it`, `gpt-oss-120b`), подивитися тексти в «Проміжна» і
   вибрати. Це 15 хвилин, які визначать якість 3912 карток.

## Приватність — що можна і чого не можна віддавати

Безкоштовні тарифи майже поголовно вчаться на переданих даних. Google AI Studio: дані
використовуються для навчання поза UK/CH/EEA/EU — Україна сюди не входить, отже так, вчаться.
Mistral Experiment: згода на навчання обов'язкова. OpenRouter free-варіанти моделей: логування
промптів звичайна річ.

Для цього проєкту ризик низький, бо `ai_enrich` віддає лише довідкові дані BM Parts — артикул,
бренд, OEM, аналоги, характеристики, сумісність. Це публічна інформація, вона й так у картці
на Prom. Але межа має бути жорсткою, тому в коді стоїть коментар-запобіжник: у `facts`
**ніколи** не додавати ціну, собівартість і дилерські умови. Ключі, `GCP_SA_KEY` і
`AUTONOVA_COOKIE` до AI-шару не потрапляють узагалі.

## Побічна користь зі списку

Cloudflare Workers AI і Google дають безкоштовні **embedding**-моделі. Це не наділення, але
цікаво для іншої задачі проєкту: зіставлення назв позицій каталогу з назвами в прайсах
постачальників, коли артикули не збігаються — зараз це робиться нормалізацією рядків
(`common/normalize.py`), а вектори витягнули б ще частину з тих 88 позицій, яким постачальника
не знайшлося в останньому прогоні.

## Що саме взято з `cheahjs/free-llm-api-resources` (звірка 26.07.2026)

Це репозиторій-**довідник**, а не бібліотека: там `README.md` зі списком провайдерів,
їхніх моделей і лімітів, плюс службовий скрипт, який цей README перегенеровує.
Імпортувати звідти нічого — ні клієнта, ні функції виклику моделі в ньому немає.
Тому взято **дані**, а код сходів (`adding/ai_layer.py`) наш власний:

| Що взято зі списку | Куди лягло в коді |
|---|---|
| Перелік провайдерів із безкоштовним тарифом | ключі словника `PROVIDERS` |
| URL OpenAI-сумісних ендпойнтів | 1-й елемент кортежу `PROVIDERS[...]` |
| Ідентифікатори моделей | 3-й елемент — **кортеж кандидатів** |
| requests/minute і tokens/minute | 4-й елемент — мінімальна пауза між запитами |
| requests/day | 5-й елемент — добовий стоп (`_quota_left`) |
| Хто вчиться на переданих даних | розділ «Приватність» вище |

Чого в списку **немає** і що довелось вирішувати самому: порядок сходів
(`ORDER` — спершу якість, далі ємність, платний Anthropic останнім), поведінка
при 429 (`Retry-After` -> cooldown -> наступний провайдер), і те, що ліміт
tokens/minute, а не requests/minute, часто є справжньою стелею — при ~2500
токенах на картку саме він задає паузу.

### Назви моделей — слабке місце, тому вони списком

Провайдери перейменовують моделі частіше, ніж ми правимо код. Одна зашита назва
означає, що після перейменування сходинка вмирає **мовчки**: ключ на місці,
добовий ліміт не вибрано, а кожен запит повертає 404. Тому кожен провайдер тримає
кортеж кандидатів: перший 404 -> пробуємо наступну назву, робочу запам'ятовуємо
на весь прогін і показуємо у звіті (`usage_report`, рядок «Останній запуск» у пульті).
Ціна питання — одна зайва відповідь 404 раз на прогін.

Помилки ліміту (429/402/403) через цей механізм **не проходять**: там треба чекати,
а не міняти назву моделі, тож вони летять далі й ставлять провайдера в cooldown.
Це закріплено тестом `tests/test_ai_ladder.py::test_rate_limit_is_not_swallowed_by_model_fallback`.

Примусово задати модель можна завжди, без правки коду:
`AI_MODEL_GEMMA=gemma-3-12b-it`, `AI_MODEL_GROQ=llama-3.3-70b-versatile` і так далі.
Порядок сходів — змінною `AI_PROVIDERS`, через кому.

## Повторна звірка зі списком (26.07.2026, друге читання)

Власник ще раз надіслав посилання на список — перечитав його рядок за рядком і
порівняв із таблицею `PROVIDERS`. Знайшлось три розбіжності, усі виправлені.

**1. Пропущена ціла сходинка — Cloudflare Workers AI.** У списку вона є
(«10 000 нейронів/добу», 40+ моделей), у нас її не було. Додано під ключем
`cloudflare`. Особливість: ID акаунта стоїть у самій **адресі**, а не в заголовку,
тому цій сходинці потрібні ДВА секрети — `CF_API_TOKEN` і `CF_ACCOUNT_ID`.
Якщо є лише токен, провайдер у сходи не потрапляє взагалі (`_ready`), бо інакше
кожен його запит повертав би 404 і це виглядало б як «нема такої моделі».

Нейрон — не запит: скільки їх з'їдає одна картка, залежить від моделі, і точної
арифметики тут не побудувати. Тому добовий ліміт свідомо занижений до 200 — краще
недобрати безкоштовного, ніж довбити провайдера в стелю; коли нейрони скінчаться,
прилетить 402 і сходи підуть далі самі.

**2. Scaleway: моделі зі списку змінились.** Кандидат `mistral-nemo-instruct-2407`
у переліку відсутній. Замінено на ті, що там є зараз: `llama-3.3-70b-instruct`,
`gpt-oss-120b`, `mistral-small-3.2-24b-instruct-2506`. Уточнено й характер ліміту:
у Scaleway це **1 млн токенів разово**, а не щодоби, тобто ~400 карток усього.

**3. Два ліміти в коментарях були неточні:**

| Було | У списку насправді | Наслідок |
|---|---|---|
| Groq: 12 000 tokens/minute | **8 000** на `gpt-oss-120b` | пауза 20 с і так із запасом, але цифра в коментарі вводила в оману |
| Cerebras: «місячна стеля токенів» | **1 000 000 токенів/добу** | звідси й береться ліміт 400 карток: 1 000 000 / ~2500 |

Що звірено й **збіглося**: Gemini 3.1 Flash-Lite (500/добу, 15 rpm) проти звичайного
Flash (лише 20/добу — тому flash-lite і стоїть першим); Gemma (14 400/добу, 30 rpm,
15 000 tpm — саме tpm задає паузу 10 с); OpenRouter (20 rpm, 50/добу); Cohere
(20 rpm, 1000 запитів/**місяць** -> наші 33/добу); Mistral (1 запит/сек);
NVIDIA NIM (40 rpm). Назви моделей Gemini 2.5 Flash / 2.5 Flash-Lite і Gemma 3 27B/12B
у списку присутні — наші запасні кандидати валідні.

**Свідомо НЕ взято** зі списку: HuggingFace Inference (0,10 $ кредитів на місяць —
надто мало), Vercel AI Gateway (5 $/місяць кредитами — це не безкоштовний тариф,
а кредити), Mistral Codestral (модель для коду, не для карток товару), OpenCode Zen
(моделі без задекларованих лімітів). Кожне з них можна додати рядком у `PROVIDERS`,
якщо знадобиться.
