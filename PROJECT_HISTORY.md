# PROJECT_HISTORY.md — Prom Autopilot (Visimics)

> **ЧИТАЙ ЦЕЙ ФАЙЛ ПЕРШИМ на КОЖНІЙ обробці.** Це канонічна пам'ять проєкту.
> Не перепитуй те, що тут зафіксовано. Після кожної суттєвої зміни — онови розділ
> «Журнал змін» і відповідні секції.
>
> Останнє оновлення: 2026-07-18

---

## 0. Що це за проєкт і мета

**Visimics** — український інтернет-магазин автозапчастин на **Prom.ua** (+ сайт visimics.com.ua).
**Prom Autopilot** — автоматизація ціноутворення та наповнення каталогу.

Головна задача системи: щодоби **проставляти актуальні ціни та наявність** для всього
каталогу Prom (≈3913 позицій), беручи собівартість із прайсів постачальників, і
**записувати результат назад у Google Таблицю**, з якої Prom тягне фід по URL.

Мета власника по грошах: вивести магазин у стабільний плюс (орієнтир — від $1K/міс чистого).

---

## 1. Репозиторій і точки входу

- **Репозиторій:** `dsegodin268-stack/prom-autopilot` (GitHub, приватний).
- **Головний скрипт:** `main.py` (корінь репо) — репрайсер усього каталогу.
- **Запуск:**
  - **Нічний (cron):** workflow `.github/workflows/nightly.yml`, cron `23 5 * * *`
    (~08:23 за Києвом) + `workflow_dispatch`.
  - **Ручна кнопка** в Google Таблиці (Apps Script) → `ghDispatch` → той самий
    `nightly.yml` (workflow_dispatch). Тобто ручний і нічний режими — це ОДИН workflow.
- **Мова/рантайм:** Python 3.11, `pip install -r requirements.txt`.

---

## 2. Google Таблиця (HUB)

- **ID таблиці (HUB):** `1pesHiOHDq2Y4FYQECakfhIJlq08bg5_Pkm9e2YEDoic`
- **Читання CSV (gviz):**
  `https://docs.google.com/spreadsheets/d/{ID}/gviz/tq?tqx=out:csv&sheet={назва}`

### Вкладки
- **`Export Products Sheet`** — БОЙОВА вкладка у форматі ЕКСПОРТУ Prom (31 колонка A–AE).
  **Саме її Prom тягне по URL-фіду.** `main.py` пише ціну+кількість+наявність прямо сюди.
  - Індекси колонок, які використовує `main.py` (0-based):
    `C_CODE=0` (A, Код_товару), `C_NAME=1` (B, Назва_позиції),
    `C_PRICE=8` (I, Ціна), `C_AVAIL=15` (P, Наявність), `C_QTY=16` (Q, Кількість).
  - Повний перелік 31 колонки — див. розділ 7.
- **`Звіт`** — звіт репрайсера. **Тут лежать 796 позицій без ціни зі статусом
  «Немає постачальника».** З ними працюємо ПРЯМО ТУТ. (Рішення власника: НЕ створювати
  окремих вкладок під 796.)
- **`Staging_Prom`** — повний рядок Prom для нових позицій (гейт наповнення перед Export).
- **`BMParts`** — [НОВА, Задача 1] дзеркало наявності BM Parts у Prom-форматі,
  оновлюється щодоби (нове додається, зникле прибирається).

---

## 3. Джерела даних (постачальники) — ЩО і ЯК підключено

Пріоритет у водоспаді — зверху вниз (перше влучення виграє).

1. **BMW Sheet** — прайс собівартості BMW (Google-джерело).
2. **Porsche Sheet** — прайс Porsche.
3. **VAG** — джерело VAG (через BM Parts); додане в двигун цін. Task #29 (готово).
4. **AutoNova-Drive** — файл/архів (zip) дилерських цін Autonova **на Google Drive**.
   - Секрет: **`AUTONOVA_FOLDER_ID`** (папка на Drive).
   - **Працює давно і стабільно.** Скрипт у репо тягне ціни з цього Drive-файлу.
   - НЕ ПИТАТИ про це — вирішено й працює.
5. **`autonova_web_cache.csv`** (корінь репо) — 322 дилерські ціни Autonova, зібрані
   веб-парсером і закомічені (commit #88). Формат: TAB-separated
   `code\tcost\tqty\tpresence`. Бренд у best: «Авто-web». Читається `pull_autonova_cache()`.
6. **Autonova веб-парсер** (жива перевірка на сайті) —
   API `catalogue-api.autonovad.ua/api/products/{code}_{brandId}/extended-offers`.
   Потрібен `brandId` (72=BMW, 56=Mercedes, 59=Mann); без бренду віддає порожнє.
   CORS: дозволяє origins docs.google.com + autonovad.ua з credentials.
7. **BM Parts API** — `api.bm.parts` (див. розділ 6).

### Захист цін
- **`anchor_prices_30-06.csv`** (корінь репо) — якірні ціни, захист «не нижче 60%»
  (щоб репрайсер не обвалив ціни в рази). Закомічено.

---

## 4. Водоспад ціноутворення (цільова логіка для 796 без ціни — Задача 2)

Для кожного артикула по черзі, до першого влучення:
1. **Основні прайси:** BMW Sheet, Porsche Sheet, VAG, AutoNova-Drive.
2. Не знайшлось → **веб-парсер Autonova** (сайт).
3. Не знайшлось → **BM Parts API**.
4. Ніде немає → статус **«Ручне коригування ціни»** (пишеться у вкладку `Звіт`,
   де позиція вже є зі статусом «Немає постачальника»).

Мета: зменшити 796 до мінімуму.

---

## 5. Ключові файли й функції `main.py`

- `pull_autonova_cache(best, instock, path=None)` — читає `autonova_web_cache.csv`
  (TAB-sep) у `best` з брендом «Авто-web».
- `_expand_code(code)` — розкриває короткий суфікс коду (спліт по «-»).
- `pull_pairs_from_best(...)` — пари (BMW-артикул до тире); матч (1) число до тире як
  BMW-артикул, (2) весь нормалізований код, (3) сума пари.
- `_bmparts_price_map(brands=None)` — будує мапу цін BM Parts. Якщо `brands=None` →
  бере всі авто-марки з `catalog/cars/brands/`, ітерує `prom_price_csv(brand, whs)`,
  дедуп по найнижчій ціні. Друкує `[bmparts] мапа: N унікальних артикулів по K марках`.
- `pull_bmparts(codes, best, instock, brands=None)` — матч по ключу «до тире»
  (`re.split(r"[-–—]", code)[0]`).

### `bmparts.py` (корінь репо; клієнт BM Parts)
- Клас `BMParts(token)`, `.s` (requests Session), `.warehouses()`,
  `.prom_price_csv(brand_name, warehouses)`, `.get_product`, `.search_uuid`.
- `__main__` з аргументом `BULK:BMW` друкує склади + перші 15 рядків prom_price_csv
  (ВКЛЮЧНО з заголовком).

### Enrichment (наповнення новими позиціями)
- `enrich_to_sheet.py`, `enrich_add.py` — пакетне збагачення BM Parts → всі поля Prom.
- Workflow `add_positions.yml` (inputs: supplier, count).
- Apps Script: панель «Додавання» + меню-кнопка + гейт підтвердження → Export.
- Детермінований двигун опису: категорія+вага+кроси+характеристики+30 ключовиків+мета-підпис;
  AI-хук (GitHub Models) для важких позицій, gated на токен.

---

## 6. BM Parts API (api.bm.parts) — довідник

- **Авторизація:** заголовок `Authorization: API-KEY` (значення з секрету **`BMPARTS`**).
- **Ліміт:** 10 000 запитів/год.
- **Важливо:** прайсові ендпоінти віддають **ТІЛЬКИ товари в наявності** (немає залишку → не повертається).
- Офіційна документація: `https://developer.bm.parts/api/v2/prices.html`

### Ендпоінти прайсу
- **`POST /prices/list`** — увесь наявний прайс. Параметри: `id_type`, `currency`,
  `warehouses[]`, `products[]`, `products_type` (uuid|code), `car_brand` (рядок або
  **список** `["BMW","Renault"]`), `brand` (бренд запчастин), `nodes[]`,
  `format` (`csv`|`json`), `full_product_name`, `in_waiting`, `multiplicity`, `no_discounts`.
  → **Всі бренди ОДНИМ запитом.** Формат GENERIC (ID/артикул/бренд/ціна/залишок), НЕ Prom-колонки.
- **`POST /prices/prom/{brand_name}`** — прайс у **форматі імпорту Prom.ua**.
  `brand_name` — обов'язковий (авто-марка). Параметри: `currency`, `warehouses[]`.
  → Формат Prom, але **ПО ОДНІЙ марці за запит**. Поле `Опис` = назва товару.

### Інші корисні ендпоінти
- `GET /catalog/cars/brands/` — список авто-марок.
- `GET /catalog/brands` — бренди виробників запчастин.
- `POST /lists/photos_link` — фото.

### Заголовок, який реально віддає `/prices/prom` (18 колонок ядра, захоплено на BMW-прогоні)
```
Код_товару, Назва_позиції, Назва_позиції_укр, Опис, Опис_укр,
Посилання_зображення, Тип_товару, Виробник, Країна_виробник,
Ціна, Валюта, Кількість, Наявність, Одиниця_виміру,
Ідентифікатор_товару, Де_знаходиться_товар,
Назва_Характеристики, Значення_Характеристики, …
```

---

## 7. Формати Prom: IMPORT vs EXPORT (важлива відмінність)

`/prices/prom` = формат **ІМПОРТУ** Prom (18 кол.). `Export Products Sheet` = формат
**ЕКСПОРТУ** Prom (31 кол.). **Вони НЕ ідентичні** — різний склад і порядок колонок.

### Export Products Sheet — 31 колонка (A–AE)
A Код_товару · B Назва_позиції · C Назва_позиції_укр · D Пошукові_запити ·
E Пошукові_запити_укр · F Опис · G Опис_укр · H Тип_товару · I Ціна · J Валюта ·
K Одиниця_виміру · L Мінімальний_обсяг_замовлення · M Оптова_ціна ·
N Мінімальне_замовлення_опт · O Посилання_зображення · P Наявність · Q Кількість ·
R Номер_групи · S Назва_групи · T Посилання_підрозділу · U Можливість_поставки ·
V Термін_поставки · W Спосіб_пакування · X Спосіб_пакування_укр ·
Y Унікальний_ідентифікатор · Z Ідентифікатор_товару · AA Ідентифікатор_підрозділу ·
AB Ідентифікатор_групи · AC Виробник · AD Країна_виробник · (AE порожня)

### Мапінг /prices/prom → Export
- **Збігаються за назвою (15):** Код_товару, Назва_позиції, Назва_позиції_укр, Опис,
  Опис_укр, Посилання_зображення, Тип_товару, Виробник, Країна_виробник, Ціна, Валюта,
  Кількість, Наявність, Одиниця_виміру, Ідентифікатор_товару.
- **Є в Export, немає в prom (16):** Пошукові_запити(_укр), Мінімальний_обсяг_замовлення,
  Оптова_ціна, Мінімальне_замовлення_опт, Номер_групи, Назва_групи, Посилання_підрозділу,
  Можливість_поставки, Термін_поставки, Спосіб_пакування(_укр), Унікальний_ідентифікатор,
  Ідентифікатор_підрозділу, Ідентифікатор_групи.
- **Є в prom, немає в Export (3+):** Де_знаходиться_товар, Назва_Характеристики,
  Значення_Характеристики (+хвіст характеристик).

---

## 8. Прийняті рішення власника (LOCKED — не перепитувати)

1. **Вкладка `BMParts`:** повний каталог Prom-формату, але **тільки наявність** з BM Parts API.
   **Автоматична щодобова синхронізація** — дзеркало: нова позиція з'явилась → додається;
   зникла → прибирається. Завжди актуальна база. Не плодити позиції просто так.
2. **796 позицій без ціни:** ніяких окремих вкладок. Вони вже у `Звіт` зі статусом
   «Немає постачальника». Працювати з ними ТАМ. Не створювати зайвих вкладок і плутанини.
3. **AutoNova-Drive:** доступний, ціни оновлюються з Google Drive через репо-скрипт.
   Давно працює. НЕ ПИТАТИ.
4. **Prom тягне саме `Export Products Sheet`** — підтверджено.
5. **Захист цін:** не нижче 60% (anchor_prices).

---

## 9. Секрети та змінні CI (GitHub → Settings)

**Secrets (наявні):** `AUTONOVA_FOLDER_ID`, `BMPARTS`, `GCP_SA_KEY`, `MAIL_PASS`,
`MAIL_USER`, `PROM_API_KEY`.
(`AUTONOVA_COOKIE` — відсутній; веб-парсер cookie-режим не активний у CI.)

**Variables:** `BMPARTS_BRANDS`, `LIVE`, `LIVE_ONLY`, `LIVE_LIMIT`.

**`nightly.yml` env:** GCP_SA_KEY, MAIL_USER, MAIL_PASS, PROM_API_KEY, AUTONOVA_FOLDER_ID,
AUTONOVA_COOKIE, BMPARTS_TOKEN=`${{ secrets.BMPARTS }}`, BMPARTS_BRANDS=`${{ vars.BMPARTS_BRANDS }}`,
BM_MIN_INTERVAL="0.8", LIVE, LIVE_ONLY, LIVE_LIMIT.

> Примітка: у коді змінна оточення зветься `BMPARTS_TOKEN`, але значення береться
> із секрету `BMPARTS`. Це НЕ помилка — так налаштовано свідомо.

---

## 10. Поточний стан

- Каталог: **3913 позицій**.
- Останній верифікований прогін: **3117 з ціною / 796 без ціни** («Немає постачальника»).
- `autonova_web_cache.csv`: 322 рядки (commit #88).
- Ручна кнопка = nightly.yml dispatch — працює.

---

## 11. Активні задачі (поточний спринт)

- **Задача 1 (BMParts tab):** скрипт синхронізації наявності BM Parts → вкладка `BMParts`
  у Prom-форматі; щодобовий автозапуск; дзеркало (add/remove).
- **Задача 2 (796 waterfall):** строгий водоспад (розділ 4); залишок → статус
  «Ручне коригування ціни» у `Звіт`.

---

## 12. Технічні застороги (з досвіду)

- **Sandbox-маунт обрізає `main.py` ~385 рядком** — bash `py_compile` дає хибний SyntaxError
  на межі обрізки. Джерело істини — file-tools Read; фінальний гейт синтаксису — CI.
- BM Parts brand-less prom-ендпоінт → порожньо; потрібен бренд (авто-марка) у дорозі.
- Content-filter браузера ріже повернення з токенізованими URL/кодами (~1400 симв.).
- Для повного зліпку всіх брендів у Prom-форматі: `/prices/prom` — по марці за запит;
  `/prices/list` — усі бренди одним запитом (але generic-формат → потрібна трансформація).

---

## 13. Журнал змін

- **2026-07-18** — Створено PROJECT_HISTORY.md. Зафіксовано остаточні рішення власника
  (розділ 8). Старт Задачі 1 (BMParts tab) і Задачі 2 (796 waterfall).
- Раніше — commit #88: `autonova_web_cache.csv` (322 ціни). anchor_prices_30-06.csv
  (захист 60%). main.py пише ціну+к-сть у Export. BM Parts/Autonova інтеграції.
