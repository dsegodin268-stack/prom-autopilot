# PROJECT_HISTORY.md — Prom Autopilot (Visimics)

> **ЧИТАЙ ЦЕЙ ФАЙЛ ПЕРШИМ на КОЖНІЙ обробці.** Це канонічна пам'ять проєкту.
> Не перепитуй те, що тут зафіксовано. Після кожної суттєвої зміни — онови розділ
> «Журнал змін» і відповідні секції.
>
> Останнє оновлення: 2026-07-29

---

## ⚠️ РЕСТРУКТУРИЗАЦІЯ 2026-07-24 (читати перед розділами 1 і 5)

Монолітний `main.py` РОЗІБРАНО на модулі (див. `MIGRATION_2026-07-24.md`):
`common/` (config, sheets, pricing, normalize, prom_format, bmparts_client),
`repricing/` (нічний перерахунок цін: run, guard, report, export_writer, overrides,
sources/), `adding/` (конвеєр додавання позицій: run, review, card_builder, ai_layer,
rules, canon, validator, completeness, groups, panel), `bmparts_mirror/`, `competitors/`,
`tools/`, `tests/` (354 тести, ганяються CI на кожен push). Воркфлоу лежать у
`.github/workflows/`: `nightly.yml` (ціни), `add.yml` (додавання), `ci.yml`,
`sync_bmparts.yml`, `bmparts_probe.yml`. Розділи 1 і 5 нижче описують СТАРУ структуру —
лишені як історія; актуальна точка входу цін `python -m repricing.run`, додавання
`python -m adding.run`.

---

## 0. Що це за проєкт і мета

**Visimics** — український інтернет-магазин автозапчастин на **Prom.ua** (+ сайт visimics.com.ua).
**Prom Autopilot** — автоматизація ціноутворення та наповнення каталогу.

Головна задача системи: щодоби **проставляти актуальні ціни та наявність** для всього
каталогу Prom (≈3913 позицій), беручи собівартість із прайсів постачальників, і
**записувати результат назад у Google Таблицю**, з якої Prom тягне фід по URL.

Мета власника по грошах: вивести магазин у стабільний плюс (орієнтир — від $1K/міс чистого).

---

## 1. Репозиторій і точки входу (історична секція; див. блок «Реструктуризація» вище)

- **Репозиторій:** `dsegodin268-stack/prom-autopilot` (GitHub, приватний).
- **Головний скрипт:** ~~`main.py`~~ → з 24.07 `repricing/run.py` (ціни) і `adding/run.py` (додавання).
- **Запуск:**
  - **Нічний (cron):** workflow `.github/workflows/nightly.yml` + `workflow_dispatch`.
  - **Ручна кнопка** в Google Таблиці (Apps Script, меню «⚙️ Prom») → ghDispatch → той самий
    `nightly.yml`. Тобто ручний і нічний режими — це ОДИН workflow.
- **Мова/рантайм:** Python 3.11, `pip install -r requirements.txt`.

---

## 2. Google Таблиця (HUB)

- **ID таблиці (HUB):** `1pesHiOHDq2Y4FYQECakfhIJlq08bg5_Pkm9e2YEDoic`
- **Читання CSV (gviz):**
  `https://docs.google.com/spreadsheets/d/{ID}/gviz/tq?tqx=out:csv&sheet={назва}`

### Вкладки (актуально на 29.07)
- **`Export Products Sheet`** — БОЙОВА вкладка у форматі ЕКСПОРТУ Prom. **Саме її Prom
  тягне по URL-фіду.** Репрайсер пише сюди Ціну (I), Наявність (P), Кількість (Q).
- **`Звіт_Ціни`** — журнал репрайсера (формується ПАРАЛЕЛЬНО з Export, ніколи не свіжіший за нього).
- **`Огляд_Додавання`** — кандидати конвеєра додавання (A–O; N=Взяти, O=Статус;
  **Q1 = «Останній запуск»** — читає меню «⚙️ Prom»).
- **`Staging_Prom`** — картки рівнів 2–3 (гейт перед Export).
- **`Export Groups Sheet`** — групи Prom.
- **`Пульт_Додавання`** — ВИДАЛЕНА власником 29.07; більше не створюється автоматично
  (повернути: add.yml з mode=panel).

---

## 3. Джерела даних (постачальники)

1. **BMW Sheet** («Прайс Баварии Моторс», ID у common/config.py) — вкладки: наяв /
   чекати 2-3д / Під замовлення 15 днів.
2. **Porsche Sheet** («Порше Інтер Авто Україна») — вкладки: склад / аксесуари (force available).
3. **AutoNova-Drive** — zip дилерських цін на Google Drive (секрет `AUTONOVA_FOLDER_ID`). Бренд «Авто».
4. **BMW-пари / добір за ключем** — без мережі, з уже зібраного (див. Журнал 29.07).
5. **autonova-web** — живий catalogue-api під дилерською кукі `AUTONOVA_COOKIE` (бренд «Авто-web»);
   правило «тільки оригінал» (24.07). Крос-перевірка «під замовлення» позицій.
6. **BM Parts API** — `/prices/list`, останній щабель (бренд «BM Parts»).

### ПРІОРИТЕТ ПРАЙСУ BMW (власник, 29.07 — LOCKED)
Позиції з прайсу «Баварія Моторс» несуть lock: 0=«наяв», 1=«чекати 2-3д»,
2=«під замовлення 15 днів». Правила (`repricing/sources/base.py`):
- lock 0/1 не перебиває НІХТО — ціна BMW завжди перша, autonova навіть не питається;
- lock 2 віддається лише ШВИДКОМУ джерелу з терміном ≤5 днів (autonova); повільніше —
  лишається ціна BMW «під замовлення 15 дн»;
- позиції без lock — старе правило «найдешевша перемагає», крос-перевірка «швидший виграє».

### Захист цін
- Якірний захист (guard) — сильне зниження/нижче якоря без конкурента = УТРИМАНО.
- «тільки оригінал» на autonova — аналоги не підставляються ніколи.

---

## 4. Конвеєр ДОДАВАННЯ позицій (adding/)

- `add.yml` (workflow_dispatch): mode=review (кандидати в Огляд) / enrich (збагатити
  відмічені «Взяти») / panel (створити пульт свідомо). Поля воркфлоу = основне керування
  після видалення пульта.
- ЧОТИРИ шари якості картки: `card_builder.enforce_limits()` (шлюз) →
  `validator` (оцінка) → `ai_layer.audit_card()` (друга думка, не блокує) →
  `card_builder.repair_card()` (другий прохід).
- Рівні готовності: 1 → Export; 2 (без чогось, але з фото) → Staging; 3 (без фото) → Staging.
- ШІ ніколи не чіпає: Код, Ціну, Валюту, Наявність, Кількість, Фото, Групу, Виробника,
  GTIN, характеристики. Ціна/собівартість/дилерські умови НІКОЛИ не йдуть у сторонні ШІ.
- `КАНОН.md` — ГЕНЕРУЄТЬСЯ (`python -m tools.rulebook` з `adding/rules.py`).

### ШІ-провайдери (ai_layer)
- Три виклики на картку: enrich → audit → repair. Три черги: `AI_PROVIDERS` /
  `AI_PROVIDERS_AUDIT` / `AI_PROVIDERS_FIX` (repo Variables; вибір черги — контекст
  `_use_queue`, підпис `_ai_call(system, user_json)` незмінний).
- Сходи: безкоштовні (Gemini, Cerebras, Groq, Mistral, Cloudflare — потрібні ДВА значення
  CF_API_TOKEN+CF_ACCOUNT_ID, Kilo :free per-IP…) → платний хвіст Vercel AI Gateway
  (`AI_GATEWAY_API_KEY`, $5/міс). Самолікування назв моделей (кортежі кандидатів),
  cooldown 429/402/403.
- **КЛЮЧІВ ЩЕ НЕМА (29.07)** — власник вводить сам у Settings → Secrets; інструкція
  `ШІ_КЛЮЧІ.md` у корені. Мінімум: `GEMINI_API_KEY` + `CEREBRAS_API_KEY`.

---

## 6. BM Parts API (api.bm.parts) — довідник

- **Авторизація:** заголовок `Authorization: API-KEY` (секрет **`BMPARTS`**; у коді env `BMPARTS_TOKEN`).
- **Ліміт:** 10 000 запитів/год. Прайсові ендпоінти віддають ТІЛЬКИ наявне.
- **`POST /prices/list`** — основний bulk (~122–126k позицій одним запитом, format csv).
  **Шапка (проба #9, 29.07): `ІД, Артикул, Бренд, Назва, Ціна ГРН` + ПО КОЛОНЦІ НА СКЛАД
  (7 складів ДАГ; «-» = нема, число = залишок). Окремої колонки «наявність» НЕМА** —
  наявність рахується сумою складських колонок (fix 29.07).
- `/prices/prom/{brand}` (per-brand) історично віддає 0 — не будувати на ньому.

---

## 7. Формат Export Products Sheet (актуальний, звірено з бойовою 26.07)

A Код_товару · B/C Назва RU/UA · D/E Пошукові_запити RU/UA · F/G Опис RU/UA ·
H Тип_товару ("r") · I Ціна · J Валюта · K Одиниця_виміру · O Посилання_зображень ·
P Наявність ("!") · Q Кількість · R Номер_групи · S Назва_групи · T Посилання_підрозділу ·
Z/AA/AB Ідентифікатори · AC Виробник · AD Країна · AF ID_групи · AL Ярлик ·
AM/AN HTML_заголовок RU/UA · AO/AP HTML_опис RU/UA · AQ GTIN · AR MPN · AS Вага ·
AX Товар_в_ProSale · AZ+ = 29 трійок характеристик.
Роздільники: `|` (мульти), `;` (ТІЛЬКИ кроси), `", "` (фото).
BMW-номери в текстах — ЗАВЖДИ суцільно (11427953129), без пробілів і дефісів.
Номер групи Prom НІКОЛИ не вигадувати — неіснуючий ID ламає весь імпорт.

---

## 8. Прийняті рішення власника (LOCKED — не перепитувати)

1. **Prom тягне саме `Export Products Sheet`.**
2. **Якірний захист** активний; звіт «Звіт_Ціни» тільки разом із записом Export.
3. **«Тільки оригінал»** на autonova (24.07): ціну з аналога не брати ніколи.
4. **Крос-перевірка** «під замовлення» на autonova (24.07) — але з 29.07 підпорядкована
   ПРІОРИТЕТУ ПРАЙСУ BMW (розділ 3).
5. **Пульт-вкладка не потрібна** (29.07): керування — поля add.yml; підсумок прогону —
   Огляд_Додавання!Q1 → меню «⚙️ Prom».
6. **Ключі ШІ вводить ТІЛЬКИ власник** у GitHub Secrets; у чат/файли/код — ніколи.
7. **`nightly.yml` не чіпати**; `requirements.txt` не вигадувати.
8. Кожна додана позиція має бути 100% знаходжуваною: повна назва / каталожний номер / авто.

---

## 9. Секрети та змінні CI (GitHub → Settings)

**Secrets (наявні):** `AUTONOVA_COOKIE`, `AUTONOVA_FOLDER_ID`, `AUTONOVA_PROXY`, `BMPARTS`,
`GCP_SA_KEY`, `GH_MODELS_TOKEN` (застарілий шлях ШІ), інші historical.
**Очікуються від власника (ШІ):** `GEMINI_API_KEY`, `CEREBRAS_API_KEY` (+ решта за
`ШІ_КЛЮЧІ.md`: GROQ/MISTRAL/CF_API_TOKEN+CF_ACCOUNT_ID/KILO/AI_GATEWAY_API_KEY…).
**Variables:** `LIVE = 1` (наразі єдина). Опційно: `AI_PROVIDERS`, `AI_PROVIDERS_AUDIT`,
`AI_PROVIDERS_FIX`, `AI_AUDIT=0`, `AI_FIX=0`, `AI_MODEL_<PROV>`.

---

## 10. Поточний стан (29.07.2026)

- Каталог: 3912 кодів. Звіт_Ціни: 3912 рядків; 3817 позицій із постачальником.
- CI зелений: 354 тести (#76).
- Меню «⚙️ Prom» у таблиці: «🕘 Останній запуск» (з Огляд!Q1) + Оновити ціни +
  Додати позиції (тепер живий add.yml; стара кнопка била в неіснуючий add_positions.yml
  і завжди давала 404) + Почистити огляд.
- ШІ-конвеєр готовий, але ФАКТИЧНО ВИМКНЕНИЙ: нема жодного ключа провайдера
  (add #25: «ШІ не перевіряв: нема ключа жодного провайдера»).
- Нічний прогін ~1.5–2 год: основний запис цін у перші ~10 хв, далі повільна
  крос-перевірка autonova (~2400 позицій × ~2с) з інкрементальним дописом.
- Картка `31306791712` (ряд 3962) досі не імпортована в кабінет Prom.

---

## 13. Журнал змін

- **2026-07-29 — ПРІОРИТЕТ ПРАЙСУ BMW + чесні мітки + наявність BM Parts + пульт геть:**
  - Скарга власника: BMW-позиції перевірялись на autonova і дорожчали; Audi/VW-коврики
    в звіті стояли «з прайсу BMW»; ВЕСЬ BM Parts був «під замовлення 15 дн».
  - Доведено пошуком по живих прайсах: артикулів 8W7061221B немає ні в BMW, ні в Porsche
    книгах → мітку «BMW» ставив крок пар. Лог nightly #84: `[bmparts] додано 35 кодів
    (у наявності: 0)` → проба LIST показала складські колонки без колонки «наявність».
  - Фікси: lock 0/1/2 у `base.py`+`bmw_porsche_sheets.py`; recheck пропускає lock≤1 і
    вимагає ≤5 днів для lock=2 (`autonova_web.py`); чесний донор «X (ключ)» і ×2 лише
    для справжніх 11-цифрових BMW-пар (`bmw_pairs.py`); наявність BM Parts = сума
    складських колонок (`bmparts_prices.py`); +14 тестів (354 зелених, ci #73–76).
  - Пульт: вкладка не створюється автоматично (`panel.py`, `run.py`); статус прогону →
    `Огляд_Додавання!Q1` з датою (Europe/Kyiv); `Код.gs` ПЕРЕПИСАНО: пункт
    «🕘 Останній запуск», живий dispatch add.yml, чистка Огляду A2:O, старі гейти видалено.
  - `tools/bmparts_probe.py`: режим LIST (шапка /prices/list). Верифікаційний прогін
    nightly #85 запущено; дублікати #86/#87 (подвійний клік кнопки) скасовано.
- **2026-07-28 — ШІ в код + жорсткі інструкції + ключі:**
  - `ai_layer.py`: сходинки Kilo (:free, per-IP — запасна) і Vercel AI Gateway
    (`AI_GATEWAY_API_KEY`, платний хвіст наприкінці ORDER — наявність секрета і є
    вимикачем витрат); ТРИ черги main/audit/fix через `_use_queue` (контекст, не kwarg —
    ci #68–70 були червоні через kwarg, полагоджено в a861bde, ci #71 зелений);
    жорсткі правила мови/знаходжуваності/меж у промптах, `_AUDIT_HARD` без чисел
    (числа — тільки з rules.py). `add.yml`: нові env-ключі. `ШІ_КЛЮЧІ.md` створено.
    Сухий прогін add #25 — конвеєр цілий, ШІ чекає ключів.
- **2026-07-26/27 — КАНОН і чотири шари:** канонічний шаблон картки з бойового Export
  (`adding/canon.py`, 3 роздільники, 29 трійок характеристик, реальні ID груп);
  `adding/rules.py` — ЄДИНИЙ машинний звід правил Prom+Google; `КАНОН.md` генерується;
  аудит ШІ на всю картку + другий прохід repair; BMW-номери суцільно; ~340 тестів.
- **2026-07-24 — РЕСТРУКТУРИЗАЦІЯ:** main.py → common/repricing/adding/bmparts_mirror/
  competitors/tools/tests (див. MIGRATION_2026-07-24.md). Якірний захист УВІМКНЕНО;
  крос-перевірка autonova ПІСЛЯ основного запису, інкрементальна; «тільки оригінал»;
  звіт паралельно з Export; IMAP-гілку і статичний кеш autonova видалено.
- **2026-07-19 — PORSCHE brandId 81 знайдено** (266/343 залишку мали пропозиції; фікс
  AUTONOVA_BRANDS + guess WAP*/9-шасі→81). BM Parts матчинг нормалізовано `_nkey`
  (дефісні артикули; +25 позицій).
- **2026-07-18 — VAG brandId 1 знайдено** (залишок 784→370, +414 кодів autonova-web);
  cookie з `\n` полагоджено; `/prices/list` як bulk (124k+); sync-bmparts щодоби;
  PROJECT_HISTORY.md створено.
- Раніше — commit #88: autonova_web_cache.csv (322 ціни; видалений 24.07), anchor_prices
  (захист 60%), перші інтеграції BM Parts/Autonova.
